Plan: 1 to modify.

Summary by type:
  tables: 1 to modify

Tables:
  ~ a
    ~ current_revision (column_rename)

DDL to be executed:
--------------------------------------------------

ALTER TABLE a RENAME COLUMN revision TO current_revision;
